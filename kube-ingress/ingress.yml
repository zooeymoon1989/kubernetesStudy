apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: simple-ingress
spec: 
  rules:
  - host: alapaca.example.com
    http:
      paths:
      - pathType: Prefix
        path: /golang
        backend:
          service:
            name: simple-golang
            port:
              number: 80
  - host: alapaca.example.com
    http:
      paths:
      - pathType: Prefix
        path: /node
        backend:
          service:
            name: simple-node
            port:
              number: 80
